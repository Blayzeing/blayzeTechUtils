package classes.env;

import classes.math.Point;

//This is defined as package-level because only env classes need to know about it.
class PolygonTriangle {
	
	public Point p1, p2, p3;
	
	public PolygonTriangle (Point point1, Point point2, Point point3)
	{
		p1 = point1;
		p2 = point2;
		p3 = point3;
	}
	
	/**
	 * Tests whether the given point is within the triangle.
	 */
	public boolean contains (double x, double y)
	{
		
	}
}